<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>WhatsApp Glass Animation</title>

<style>
  body {
    margin: 0;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: radial-gradient(circle at top, #25d36633, #0b3d2e);
    overflow: hidden;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  /* liquid glass blob */
  .glass {
    position: absolute;
    width: 420px;
    height: 420px;
    background: rgba(255,255,255,0.18);
    border-radius: 50%;
    backdrop-filter: blur(20px);
    animation: float 6s ease-in-out infinite;
  }

  .glass::before {
    content: "";
    position: absolute;
    inset: -40px;
    background: radial-gradient(circle, rgba(255,255,255,.6), transparent 70%);
    filter: blur(40px);
    opacity: .6;
  }

  @keyframes float {
    0%,100% { transform: translateY(0); }
    50% { transform: translateY(-25px); }
  }

  /* WhatsApp logo */
  .logo {
    width: 120px;
    z-index: 2;
    animation: enter 2.2s ease-out forwards;
    transform: scale(0) translateY(80px);
  }

  @keyframes enter {
    0% {
      transform: scale(0) translateY(120px);
      opacity: 0;
    }
    60% {
      transform: scale(1.2) translateY(-10px);
      opacity: 1;
    }
    100% {
      transform: scale(1) translateY(0);
    }
  }
</style>
</head>

<body>
  <div class="glass"></div>

  <!-- WhatsApp logo (SVG image) -->
  <img
    class="logo"
    src="https://upload.wikimedia.org/wikipedia/commons/6/6b/WhatsApp.svg"
    alt="WhatsApp"
  />
</body>
</html>
